#if !defined(_BST_TRAVERSAL)
#define _BST_TRAVERSAL

#include "bst.h"

void inorderTraverse(Node *root);
void preorderTraverse(Node *root);
void postorderTraverse(Node *root);

#endif // _BST_TRAVERSAL